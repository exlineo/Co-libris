import { FiltreisbnPipe } from './filtreisbn.pipe';

describe('FiltreisbnPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreisbnPipe();
    expect(pipe).toBeTruthy();
  });
});
