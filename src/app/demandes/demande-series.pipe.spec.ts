import { DemandeSeriesPipe } from './demande-series.pipe';

describe('DemandeSeriesPipe', () => {
  it('create an instance', () => {
    const pipe = new DemandeSeriesPipe();
    expect(pipe).toBeTruthy();
  });
});
