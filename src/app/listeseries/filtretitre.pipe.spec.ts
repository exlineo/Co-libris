import { FiltretitrePipe } from './filtretitre.pipe';

describe('FiltretitrePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltretitrePipe();
    expect(pipe).toBeTruthy();
  });
});
